{
    "name": "shift-manager-api",
    "version": "1.0.0",
    "description": "Azure Functions API for Shift Manager",
    "scripts": {
        "start": "func start",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@azure/cosmos": "^4.0.0"
    },
    "devDependencies": {
        "@azure/functions": "^4.0.0"
    }
}